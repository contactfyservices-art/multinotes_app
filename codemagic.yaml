workflows:
  android-workflow:
    name: Android Build
    environment:
      flutter: 3.24.5
    scripts:
      - flutter pub get
      - flutter build apk --release
    artifacts:
      - build/**/outputs/**/*.apk
